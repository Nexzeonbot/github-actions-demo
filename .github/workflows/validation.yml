name: Basic Validation

on:
  push:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run validation script
        run: |
          chmod +x validate.sh
          ./validate.sh
